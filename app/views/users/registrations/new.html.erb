<% provide(:title, 'ひとり飯 | Sign_Up')%>
<div class="main">
  <%= render 'shared/out_sidebar.html.erb'%>
  <div class="content-wrapper">
    <div class="page-title">
      <h1 class="title-font">Sign_Up</h1>
    </div>
    <div class="content-user">
      <div class="container-content user-form">
        <div class="content-document-inner">
          <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
            <%= render "users/shared/error_messages", resource: resource %>
            <div class="field">
              <%= f.label :ニックネーム %><br />
              <%= f.text_field :name, autofocus: true, autocomplete: "name", class: "form-control sign-up-form form-space" %>
            </div>
            <div class="field">
              <%= f.label :メールアドレス %><br />
              <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control sign-up-form form-space" %>
            </div>
            <div class="field">
              <%= f.label :パスワード %>
              <%= f.password_field :password, autocomplete: "new-password", class: "form-control sign-up-form pass-space" %>
            </div>
            <div class="field">
              <%= f.label :パスワード確認 %><br />
              <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control sign-up-form pass-space" %>
            </div>
            <div class="actions">
              <%= f.submit "アカウント登録", class: "form-submit base-btn" %>
            </div>
            <div class="actions">
              <%= link_to  "ログイン", new_user_session_path, class: "login-btn link-btn base-btn" %>
            </div>
          <% end %> 
        </div>
      </div>
    </div>
  </div>
</div>
